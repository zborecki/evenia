import { AllEvents } from '#components/all-events';
import Layout from '#layouts/base-layout';

const Page = () => (
  <Layout>
    <main>
      <AllEvents />
    </main>
  </Layout>
);

export default Page;
